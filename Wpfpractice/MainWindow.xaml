<Window x:Class="Wpfpractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"  
        xmlns:local="clr-namespace:Wpfpractice"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" ResizeMode="NoResize" >
    <Window.Resources>
        <SolidColorBrush x:Key="Brush" Color="Red" />
        <sys:String x:Key="StrTitle" >Pi-Techniques</sys:String> 
        <Style  TargetType="TextBlock">  
            <Setter Property="Foreground" Value="DarkRed"/>  
            <Setter Property="HorizontalAlignment" Value="Center"/>  
        </Style>  
    </Window.Resources>
    <Grid Name="PnlMainGrid" MouseUp="PnlMainGrid_OnMouseUp" Background="LightBlue" Opacity="1">
        <WrapPanel>
            <TextBlock Foreground="Blue">
                Multi
            </TextBlock>
            <TextBlock Foreground="{DynamicResource Brush}">
                Hola Multi
            </TextBlock>
            <TextBlock Text="{StaticResource StrTitle}" Style="{StaticResource {x:Type TextBlock}}">
            </TextBlock>
        </WrapPanel>
        <Button Name="BtnPlay" HorizontalAlignment="Left" Height="25" FontWeight="Bold" Click="BtnPlay_OnClick">
            First Option
        </Button>
        <TextBox Width="300" Height="50">
            hola
        </TextBox>
        <Label Content="{StaticResource StrTitle}" Margin="50" Width="200">
        </Label>
        <ComboBox Name="Box" Width="50" Height="60">
        </ComboBox>
    </Grid>
</Window>
